/**
 * Pipeline module palette. Slate/blue scheme — pipeline is the "primary" module so it
 * carries the calmest, most authoritative tone in the UI.
 */

import type { ModulePalette } from "@/lib/modules/types";

export const PIPELINE_PALETTE: ModulePalette = {
  primary: "#1e40af",
  accent: "#3b82f6",
  gradientFrom: "#dbeafe",
  gradientTo: "#1e3a8a",
};
